trinity is another fuzzing tool specifically made for syscalls. its similar to syzkaller, but quite simpler in execution.

```
git clone https://github.com/kernelslacker/trinity
cd trinity
make -j$(nproc)
```

with that, you have a trinity binary ready to go.
